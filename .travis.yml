language: node_js

notifications:
    email:
        - yiminghe@gmail.com

node_js:
  - 16.13.1

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 3.1.1
  - export PATH="$HOME/.yarn/bin:$PATH"

script:
  - npm test